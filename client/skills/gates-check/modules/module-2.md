# Module 2 : Build System

- Analyze the java build tool configured for the project
- Detect the tool engine by checking which files exist at the project root:

| File | Build tool |
|---|---|--|
| `pom.xml` | Maven |
| `build.gradle` or `build.gradle.kts` |

- Log which build tool is configured and dependencies / plugins discovered